export const feeds = {
  BuildingList: { title: 'Dubai Villas' },
  PropertyList: { title: 'Dubai Villas Properties' }
}
export const config = {
  apiUrl: 'https://fam-erp.com',
  imagePrefix: 'https://cloud.famproperties.com',
  websiteId: 1018
}

export const validFeeds = Object.keys(feeds)
export const validConfig = Object.keys(config)
